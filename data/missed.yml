#
# missed entries to retry
#
# To use, use --missed argument (e.g. `app/console mediawiki:run 3 --missed`)
#
# Each entry is relative to the out/ directory. Its assumed that what is not in
# MediaWiki’s main content namespace (e.g. Meta:Main_Page) will be in a separate git
# repository but will end up accessible as a sub folder (e.g. Meta/Main_Page).
#
# https://docs.webplatform.org/w/index.php?action=edit&title=WPD:Getting_Started/examples
#
missed:
  - en
  
